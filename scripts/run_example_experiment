#!/bin/bash

if [ $# != 3 ]; then
	echo "Usage: run_example_experiment example iterations output"
	exit 1
fi

rm -f $3

for i in $(seq 1 $2); do
	echo "Experiment iteration $i"
	scripts/run_example $1
	cat fitnesses.csv >> $3
	rm -f fitnesses.csv
done

